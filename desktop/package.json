{
  "name": "mdp-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Model Deploy Platform 桌面端：本地硬件探测 + 本地部署",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node smoke.js && node test-mlx.js && node test-trust.js && node test-install.js",
    "test:layout": "electron test-layout.js",
    "test:ui": "electron test-backend-ui.js"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
